name: CodeQL config

# Keep CodeQL focused on the rib's shipped surface (src/, dev/). Tests are
# noise for security analysis, and docs/ is a separate private Astro site.
paths-ignore:
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "test/**"
  - "docs/**"
